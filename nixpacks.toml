[phases.setup]
nixPkgs = ["python313", "nodejs_22"]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "cd frontend && npm ci && npm run build",
]

[start]
cmd = "uvicorn api.app:create_app --factory --host 0.0.0.0 --port ${PORT:-8020}"
