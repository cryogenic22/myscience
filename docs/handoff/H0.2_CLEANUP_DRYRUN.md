# H0.2 — Cleanup Transaction (DRY-RUN · awaiting owner sign-off)

**No cleanup has been executed.** This is the exact, categorized action list for owner
approval per SPEC_HANDOFF §H0.2. Every target is enumerated in the H0.1 disposition CSVs;
every at-risk object is already preserved + tested (see `PRESERVATION_README.md`). Approve
**per category** — I execute only what you sign off, re-verifying SHA/status immediately
before each op (§H0.2.1) and preserving before each removal (§H0.2.5–6).

## Preflight (before ANY category executes)
```bash
git fetch --all --prune
git worktree list --porcelain     # re-confirm 79 worktrees vs manifest
git status --short --untracked-files=all
# abort a target if its SHA/status drifted from WORKTREE/BRANCH_DISPOSITION.csv
```

## ⛔ PROTECTED — never in any cleanup
- **Baseline worktree** `mz-handoff-baseline` (branch `claude/handoff/h0-baseline`).
- **H1 security** branches/worktrees: `claude/platform/sec-001a-debug-zs-containment` (#325),
  `claude/platform/priv-001-pii-egress-gateway` (#326) — under active H1 review.
- **Primary checkout** `C:/Users/kapil/Documents/market_zero` until its untracked pile is
  dispositioned (holds `.claude/ctx` **PROTECT** session memory).
- `.claude/ctx/**` session-memory ledger (PROTECT).
- The 4 PROTECT branches + 3 PROTECT worktrees flagged in the CSVs.

## Category A — Commit the 9 already-archived reports (additive; safe)
These 9 `docs/archive/reports/*.html` are **already in the archive dir but untracked**
(`UNTRACKED_ARTIFACT_MANIFEST.csv` → `ARCHIVE-COMMIT`). No move needed — just track them + an index.
```bash
git add docs/archive/reports/*.html
# + write docs/archive/reports/INDEX.md (original path, date, reason, replacement) then commit
```
**Effect:** +9 tracked files, 0 deletions. Reversible.

## Category B — Prune merged + clean worktrees (17)
`WORKTREE_DISPOSITION.csv` → `proposed_disposition = PRUNE-eligible` (merged branch, clean tree).
```bash
# per enumerated path (native remove — NOT rm -rf):
git worktree remove <abs_path>
```
**Effect:** removes 17 worktree dirs whose branch is fully on `origin/main`. Branch deletion is Category D.

## Category C — Preserve-then-prune dirty worktrees (26 + 1 review)
`WORKTREE_DISPOSITION.csv` → `PRESERVE-then-prune` (merged branch, **dirty** tree). Patches +
untracked archives already captured + tested. On approval:
```bash
# 1) re-confirm the worktree's diff still matches its preserved patch hash, THEN
git worktree remove --force <abs_path>
```
**Effect:** 26 dirty worktrees removed (work preserved in `mz-handoff-preservation/worktrees/<name>/`).
The 18 `REVIEW` worktrees (unmerged/detached) are **held** for per-item decision — not in this batch.

## Category D — Delete branches (staged, after worktrees gone)
`BRANCH_DISPOSITION.csv`:
- **202 `PRUNE-eligible`** (merged to `origin/main`, 0 unique commits): `git branch -d <name>` (safe `-d`, refuses if not merged).
- **13 `PRESERVE(bundle)-then-review`** (local-only, bundled + tested): delete only on explicit OK, `git branch -D <name>` (force) — bundles restore them.
- **57 `REVIEW`** (on-origin, no PR) + **36 `KEEP`** (open PR): **held**, not deleted here.
```bash
# merged branches (safe):
git branch -d <name>          # for each of the 202 (exact list = CSV filter)
```
**Effect:** deletes only branches whose commits are on `origin/main` (merged) or bundled (local-only).

## Category E — PR dispositions (`PR_DISPOSITION.md`)
- **`SUPERSEDED?`** (merged-branch PRs): `gh pr close <#> --comment "<reason+successor SHA>"` — **only after** `git log origin/main..<branch>` confirms 0 unique commits (§H0.2.4).
- **STALE-SCAFFOLD #37–#70**: rebase-or-close per item after the unique-commit check.
- **#325 / #326**: **do not touch** — H1.
```bash
git log --oneline origin/main..<branch>   # MUST be empty before any close
gh pr close <#> --comment "superseded by <SHA/PR>; unique commits: none (verified)"
```

## Category F — .gitignore / retention (additive)
Add retention-ignores for generated outputs (`design-review-output/_build/`, benchmark scratch)
**without** concealing an already-unreviewed artifact (§H0.2.7). Reviewed per-line.

---

## Sign-off (owner approves per category — I execute only what's checked)
- [ ] A — commit 9 archived reports + index
- [ ] B — prune 17 merged+clean worktrees
- [ ] C — preserve-then-prune 26 dirty worktrees
- [ ] D — delete 202 merged (+ optionally 13 bundled local-only) branches
- [ ] E — close SUPERSEDED PRs after unique-commit checks (excl. #325/#326)
- [ ] F — retention .gitignore rules

**Commands executed so far in H0.2: 0.** Exact per-target command scripts can be generated
into `mz-handoff-preservation/h0.2_dryrun/` on approval, for review before any run.
